{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">Créer une conférence</button>
    {{ form_end(form) }}
{% endblock %}