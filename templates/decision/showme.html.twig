{% extends 'base.html.twig' %}
{% block body %}
    <div class="container text-center">
        <h1>Les decisions du contributor {{ contributor.login }}</h1>
        <ul>
        {% for decision in decisions %}
            <li>{{ decision.id }}</li>
        {% endfor %}
        </ul>
    </div>
{% endblock %}